// @smartstack/engine — the rules engine.
// Pure TypeScript. Zone-free: works in minutes since midnight and returns local HH:MM.
// No DOM, no Cloudflare, no user-facing strings (the UI renders rule ids through i18n).
// Filled in during the engine milestone.

export const ENGINE_VERSION = '0.1.0'
